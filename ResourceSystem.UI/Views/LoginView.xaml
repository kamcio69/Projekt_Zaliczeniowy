<UserControl x:Class="ResourceSystem.UI.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="320" d:DesignWidth="380">
    <Border Background="#F5F5F5" CornerRadius="8" Padding="32"
            BorderBrush="#E0E0E0" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="12" ShadowDepth="2" Opacity="0.15"/>
        </Border.Effect>
        <StackPanel VerticalAlignment="Center" Width="280">
            <TextBlock Text="Logowanie" FontSize="24" FontWeight="SemiBold" Foreground="#333"
                       Margin="0,0,0,28" HorizontalAlignment="Center"/>
            <TextBlock Text="Username" FontSize="13" Foreground="#555" Margin="0,0,0,6"/>
            <TextBox x:Name="UsernameBox" FontSize="14" Padding="10,8" Margin="0,0,0,16"
                     BorderBrush="#CCC" BorderThickness="1" Background="White"/>
            <TextBlock Text="Password" FontSize="13" Foreground="#555" Margin="0,0,0,6"/>
            <PasswordBox x:Name="PasswordBox" FontSize="14" Padding="10,8" Margin="0,0,0,24"
                         BorderBrush="#CCC" BorderThickness="1" Background="White"/>
            <Button x:Name="LoginButton" Content="Zaloguj" FontSize="14" FontWeight="SemiBold"
                    Padding="16,10" Background="#2196F3" Foreground="White" BorderThickness="0"
                    Cursor="Hand" Click="LoginButton_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border" Background="{TemplateBinding Background}"
                                            CornerRadius="4" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#1976D2"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Border>
</UserControl>
